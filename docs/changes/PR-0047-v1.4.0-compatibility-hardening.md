# PR-0047: v1.4.0 Compatibility Hardening

## Summary

R1.4 release train: drift detection between bot and adapter, fixture refresh for compatibility testing, enhanced route coverage with property-based testing. Backward-compatible read-only changes.

## User Impact

Operators benefit from early drift detection warning when adapter contracts change. Improved route coverage catches regressions earlier.

## Security Impact

- Command surface: unchanged (all read-only)
- RBAC or authorization: unchanged
- Secret handling: unchanged
- Data crossing Discord/bot/WebUI boundaries: unchanged
- Network exposure: unchanged

## Least Privilege

Same as v1.3.0. All routes remain read-only by contract and test. Compatibility tests do not change runtime behavior.

## Tests and Evidence

- Drift detection unit tests
- Fixture refresh scripts
- Property-based route coverage tests
- `npm run check`
- `npm audit --audit-level=moderate`
- Semgrep
- Gitleaks
- Trivy filesystem
- Docker build
- Trivy image

## Known Limitations

- Drift detection is test-time only; runtime drift detection deferred to future train

## Sources

- docs/full-release-roadmap.md (R1.x gates)
- docs/adapter-contract.md
- docs/verification.md
